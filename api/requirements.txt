# Dependencies for the Vercel serverless function.
# @vercel/python installs from the requirements.txt next to the entrypoint
# (this file), so it must list everything the app imports at runtime.

# Core (imported transitively by jobagent on startup / during a pipeline run)
httpx>=0.27
PyYAML>=6.0
Jinja2>=3.1
python-docx>=1.1
beautifulsoup4>=4.12
lxml>=5.0

# Web UI
starlette>=0.37
python-multipart>=0.0.9

# Remote database (Turso/libSQL). Prebuilt wheels exist only for CPython ≤3.12,
# so the marker skips it on newer Pythons rather than failing the whole build by
# trying to compile from Rust source. We pin Python to 3.12 via .python-version
# so this normally installs and Turso persistence works; if that pin is ever
# ignored, the app still builds and falls back to ephemeral storage (/healthz
# explains it). Keep .python-version and this marker in sync.
libsql-experimental>=0.0.30; python_version < "3.13"

streamlit
PyPDF2

pip install -r requirements.txt
streamlit run app.py
