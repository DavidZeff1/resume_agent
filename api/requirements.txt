# Dependencies for the Vercel serverless function.
# @vercel/python installs from the requirements.txt next to the entrypoint
# (this file), so it must list everything the app imports at runtime.

# Core (imported transitively by jobagent on startup / during a pipeline run)
httpx>=0.27
PyYAML>=6.0
Jinja2>=3.1
python-docx>=1.1
beautifulsoup4>=4.12
lxml>=5.0

# Web UI
starlette>=0.37
python-multipart>=0.0.9

# Remote database (Turso/libSQL) — needs a prebuilt wheel (Linux CPython ≤3.12)
libsql-experimental>=0.0.30
