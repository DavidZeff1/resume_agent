"""jobagent — an autonomous job-application *preparation* agent.

The agent automates every recoverable stage of the application pipeline
(source -> score -> tailor -> prep -> track/follow/schedule) and parks a
fully prepared, one-click-from-done application in a human review queue.

Submission is always a human action in v1. See ``job-agent-build-plan.md``.
"""

__version__ = "0.1.0"
