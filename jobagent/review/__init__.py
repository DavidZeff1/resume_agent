"""Review: the single human step — inspect a prepared application and submit it.

The agent prepares everything to one click from done; the human reviews the
pre-filled data, completes the highlighted unanswered fields on the real site,
submits there, and records it here. The agent never auto-submits in v1 (§2).
"""
