"""Tailor: select the right stored resume + cover letter for each shortlisted job.

Selection only — never generation (build plan §6.3). The agent picks from
human-authored materials; it does not author a resume. Cover-letter *templates*
may be filled with the company/role/applicant slots only.
"""
